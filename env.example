# Google Calendar API Configuration
# OBLIGATORIO: Configura estas variables con el contenido JSON completo
# Tanto para desarrollo local como para producción

# Credenciales de Google Cloud Console (descargar como JSON)
# Obtén este archivo desde Google Cloud Console > APIs & Services > Credentials
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# Token de autorización (generado después del flujo OAuth)
# Usa 'python convert_to_env_format.py' para generar este valor
GOOGLE_TOKEN_JSON={"token":"...","refresh_token":"...","id_token":"...","token_uri":"https://oauth2.googleapis.com/token","client_id":"...","client_secret":"...","scopes":["https://www.googleapis.com/auth/calendar.readonly"],"expiry":"..."}

# Alternativa: Token en base64 (si tienes un archivo pickle)
# GOOGLE_TOKEN_BASE64=base64_encoded_pickle_content

# Archivos temporales (generados automáticamente desde las variables JSON)
# NO modifiques estos valores, se generan automáticamente
GOOGLE_CREDENTIALS_FILE=/tmp/credentials.json
GOOGLE_TOKEN_FILE=/tmp/token.json

# Opcional: ID del calendario por defecto
DEFAULT_CALENDAR_ID=primary

# MongoDB Atlas Configuration
# Obtén esta URL desde MongoDB Atlas > Connect > Connect your application
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DATABASE=synco_db
